import { h1, h2, h3, p, div, pre, code, ul, li, section, strong, em, style } from "/framework/core/View/View.js";

import { test, assert } from "/framework/core/Test/Test.js";

test("Game Tests", () => {
    assert("Game tests passed");
    assert(false, "Game tests failed");
});