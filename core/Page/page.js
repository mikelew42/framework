// Stable default for the Page system — currently Page2 (page() + hash routing).
// Pinned imports use a level directly (e.g. ./1/Page1.js); this moves forward as
// levels are promoted.
export { default, page } from "./2/Page2.js";
